[project]
name = "bmschain-serial-tools"
version = "0.1.0"
description = "Tools to parse BMSCHAIN GUI serial output and export CSV for Excel."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1.5",
    "pyserial>=3.5",
]

[tool.uv]
package = false
